specVersion: "v1"
serviceId: "questforge"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "questforge-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
